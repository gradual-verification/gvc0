#use <runtime>
int add(int a, int b, int* instanceCounter);
int main();

int add(int a, int b, int* instanceCounter)
{
  return a + b;
}

int main()
{
  int sum = 0;
  int* instanceCounter = NULL;
  instanceCounter = alloc(int);
  sum = add(1, 3, instanceCounter);
  assert(sum == 4);
  return 0;
}
