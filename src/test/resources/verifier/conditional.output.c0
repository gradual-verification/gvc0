int main();
void test(int x, int y);

int main()
{
  return 0;
}

void test(int x, int y)
  //@requires ?;
{
  bool _cond = false;
  bool _cond1 = false;
  _cond = true && x > 1;
  if (x > 1)
  {
    _cond1 = _cond && x > 2;
    if (x > 2)
    {
      if (_cond && _cond1)
      {
        assert(y == 0);
      }
      //@assert y == 0;
    }
  }
}
