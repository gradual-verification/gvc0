#use <runtime>
int main();
void test(int x, int y, int* _instanceCounter);
int testCall(int a, int* _instanceCounter);

int main()
{
  int* _instanceCounter = NULL;
  _instanceCounter = alloc(int);
  return 0;
}

void test(int x, int y, int* _instanceCounter)
{
  int z = 0;
  bool _cond = false;
  bool _cond1 = false;
  bool _cond2 = false;
  bool _cond3 = false;
  bool _cond4 = false;
  bool _cond5 = false;
  bool _cond6 = false;
  bool _cond7 = false;
  _cond4 = x > 1;
  _cond4 = x > 1;
  if (x > 1)
  {
    _cond5 = x > 2;
    _cond5 = x > 2;
    if (x > 2)
    {
      z = testCall(y, _instanceCounter);
      _cond6 = y == 0;
      _cond7 = z == 0;
      _cond6 = y == 0;
      _cond7 = z == 0;
      if (z == 0)
      {
        if (_cond4 && _cond5 && !_cond6 && _cond7)
        {
          assert(x > 3);
        }
        if (_cond4 && _cond5 && _cond6 && _cond7)
        {
          assert(x > 3);
        }
      }
    }
  }
}

int testCall(int a, int* _instanceCounter)
{
  return a;
}
