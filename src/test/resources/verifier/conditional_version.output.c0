int main();
void test(int x, int y);
int testCall(int a);

int main()
{
  return 0;
}

void test(int x, int y)
  //@requires ?;
{
  int z = 0;
  bool _cond_0 = false;
  bool _cond_1 = false;
  bool _cond_3 = false;
  _cond_0 = x > 1;
  if (x > 1)
  {
    _cond_1 = _cond_0 && x > 2;
    if (x > 2)
    {
      z = testCall(y);
      _cond_3 = _cond_0 && _cond_1 && z == 0;
      if (z == 0)
      {
        if (_cond_0 && _cond_1 && _cond_3)
        {
          assert(x > 3);
        }
        //@assert x > 3;
      }
    }
  }
}

int testCall(int a)
  //@ensures ? && (a == 0 ? \result == 0 : true);
{
  return a;
}
