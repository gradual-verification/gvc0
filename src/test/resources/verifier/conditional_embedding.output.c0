#use <runtime>
int main();
int test(int input, int* instanceCounter);

int main()
{
  int* instanceCounter = NULL;
  instanceCounter = alloc(int);
  return 0;
}

int test(int input, int* instanceCounter)
{
  if (input > 0)
  {
    assert(input < 2);
  }
  if (!(input > 0))
  {
    assert(input > -2);
  }
  return input;
}
