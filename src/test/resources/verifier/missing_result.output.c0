#use <runtime>
int doSomething(int x, int* instanceCounter);
int main();
int random(int* instanceCounter);

int doSomething(int x, int* instanceCounter)
{
  return 1;
}

int main()
{
  int x = 0;
  int _ = 0;
  int _1 = 0;
  bool _cond = false;
  bool _cond1 = false;
  int* instanceCounter = NULL;
  instanceCounter = alloc(int);
  _ = doSomething(0, instanceCounter);
  _cond1 = _ == 0;
  _cond1 = _ == 0;
  x = random(instanceCounter);
  _1 = doSomething(x, instanceCounter);
  if (!_cond1 && !(_1 == 0))
  {
    assert(x == 0);
  }
  if (_cond1 && !(_1 == 0))
  {
    assert(x == 0);
  }
  return 0;
}

int random(int* instanceCounter)
{
  return 0;
}
