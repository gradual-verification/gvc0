#use <runtime>
int get(int* instanceCounter);
int main();

int get(int* instanceCounter)
{
  return 2;
}

int main()
{
  int result = 0;
  int* instanceCounter = NULL;
  instanceCounter = alloc(int);
  result = get(instanceCounter);
  result = result - 1;
  assert(result > 0);
  return 0;
}
