method main() returns ($result: Int)
  requires true
  ensures true
{
  $result := 0
}

method test(value: Int) returns ($result: Int)
  requires ? && true
  ensures $result == 0
{
  $result := value
}