#use <runtime>
struct Test;

struct Test
{
  int value;
  int _id;
};

int main();
void test(struct Test* value, int* instanceCounter);

int main()
{
  int* instanceCounter = NULL;
  instanceCounter = alloc(int);
  return 0;
}

void test(struct Test* value, int* instanceCounter)
{
  assert(value->value == 0);
}
