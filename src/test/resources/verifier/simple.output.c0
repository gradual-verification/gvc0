#use <runtime>
int main();
int test(int value, int* _instanceCounter);

int main()
{
  int* _instanceCounter = NULL;
  _instanceCounter = alloc(int);
  return 0;
}

int test(int value, int* _instanceCounter)
{
  assert(value == 0);
  return value;
}
