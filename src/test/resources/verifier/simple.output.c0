#use <runtime>
int main();
int test(int value, struct OwnedFields* _ownedFields);

int main()
{
  int* _instanceCounter = NULL;
  _instanceCounter = alloc(int);
  return 0;
}

int test(int value, struct OwnedFields* _ownedFields)
{
  assert(value == 0);
  return value;
}
