#use <runtime>
int main();
int test(int value, int* instanceCounter);

int main()
{
  int* instanceCounter = NULL;
  instanceCounter = alloc(int);
  return 0;
}

int test(int value, int* instanceCounter)
{
  assert(value == 0);
  return value;
}
