//test validation_error
// No predicate in disjunction

bool predicate(int value)
{
  return value > 0;
}

void doTest(int x, int y)
  //@requires predicate(x) || predicate(y);
{
}

int main()
{
  return 0;
}