int test(int x)
//@requires true;
//@ensures x > 0 ? \result > 0 : \result == 0;
{
  if (x > 0)
    return 1;
  else
    return 0;
}

int main()
//@requires true;
//@ensures true;
{
  test(5);
  test(-1);
  return 0;
}