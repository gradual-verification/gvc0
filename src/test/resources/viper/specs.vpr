method main() returns ($result: Int)
{
  $result := max(1, 2)
}

method max(a: Int, b: Int) returns ($result: Int)
  requires a > 0 && b > 0
  ensures $result >= a && $result >= b
{
  $result := (a > b ? a : b)
}