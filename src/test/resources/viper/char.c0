bool isAlphabet(char value)
  //@requires value != '\0';
{
  return (value >= 'a' && value <= 'z') || (value >= 'A' && value <= 'Z');
}

int main()
{
  bool alpha = isAlphabet('0');
  if (alpha) return 1;
  else return 0;
}